{
  "name": "@oldbulb/samsara-sandbox",
  "version": "0.1.0",
  "description": "Filesystem policy for attempt and proposer subprocesses: allow-list composition, landlock wrapping on Linux through the kernel, recorded enforcement mode",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/oldbulb/samsara.git",
    "directory": "packages/sandbox"
  },
  "publishConfig": {
    "access": "public"
  },
  "keywords": [
    "dsh",
    "dsh-plugin",
    "cordis",
    "samsara",
    "rsi",
    "agent-evaluation"
  ],
  "type": "module",
  "main": "lib/index.js",
  "types": "lib/types/index.d.ts",
  "exports": {
    ".": {
      "types": "./lib/types/index.d.ts",
      "default": "./lib/index.js"
    }
  },
  "files": [
    "lib"
  ],
  "engines": {
    "node": ">=22.19"
  },
  "scripts": {
    "build": "tsc -b tsconfig.json",
    "test": "vitest run --root ../.. packages/sandbox/tests"
  },
  "dependencies": {
    "@oldbulb/samsara-kernel": "workspace:*"
  }
}
