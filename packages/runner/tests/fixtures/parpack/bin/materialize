#!/usr/bin/env node
let buf = ''
process.stdin.on('data', (d) => (buf += d))
process.stdin.on('end', () => {
  for (const raw of buf.split('\n')) {
    if (!raw.trim()) continue
    const { task_id } = JSON.parse(raw)
    process.stdout.write(JSON.stringify({ task_id, ok: true, files: ['a.txt'] }) + '\n')
  }
})
