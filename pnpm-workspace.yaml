packages:
  - packages/*
# Reviewed build scripts (pnpm 11 `strictDepBuilds`): the modal SDK pulls in two
# optional native accelerators it runs without, so their builds stay off.
allowBuilds:
  cbor-extract: false
  protobufjs: false
